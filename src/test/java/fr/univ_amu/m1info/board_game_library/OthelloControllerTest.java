package fr.univ_amu.m1info.board_game_library;

import fr.univ_amu.m1info.board_game_library.controller.OthelloController;
import fr.univ_amu.m1info.board_game_library.model.BoardManager;
import fr.univ_amu.m1info.board_game_library.view.graphics.BoardGameView;
import fr.univ_amu.m1info.board_game_library.view.graphics.Color;
import fr.univ_amu.m1info.board_game_library.view.graphics.Shape;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.util.List;

import static org.mockito.Mockito.*;

public class OthelloControllerTest {
/*
    private OthelloController controller;
    private BoardGameView mockView;
    private BoardManager mockBoardManager;

    @BeforeEach
    void setUp() {
        controller = new OthelloController();
        mockView = mock(BoardGameView.class);
        mockBoardManager = mock(BoardManager.class);

        // Inject mock dependencies if needed
        controller.initializeViewOnStart(mockView);
    }

    @Test
    void testInitializeViewOnStart() {
        verify(mockView, times(1)).addShapeAtCell(3, 3, Shape.CIRCLE, Color.WHITE);
        verify(mockView, times(1)).addShapeAtCell(3, 4, Shape.CIRCLE, Color.BLACK);
        verify(mockView, times(1)).addShapeAtCell(4, 3, Shape.CIRCLE, Color.BLACK);
        verify(mockView, times(1)).addShapeAtCell(4, 4, Shape.CIRCLE, Color.WHITE);
        verify(mockView, times(1)).updateLabeledElement("BlackTurnLabel", "Black's TURN");
        verify(mockView, times(1)).updateLabeledElement("WhiteTurnLabel", "");
    }*/
}
